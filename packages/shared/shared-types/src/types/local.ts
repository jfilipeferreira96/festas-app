// ===================================
// Local — Types for room/space management
// ===================================

export interface Local {
  id: string;
  nome: string;
  capacidade: number;
  activo: boolean;
  createdAt: string;
  updatedAt: string;
}
