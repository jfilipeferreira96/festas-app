// ===================================
// Local - Types for room/space management
// ===================================

export interface Local {
  id: string;
  nome: string;
  activo: boolean;
  createdAt: string;
  updatedAt: string;
}
