import { Metadata } from "next";
import { createPageMetadata } from "@/lib/metadata";

export const metadata: Metadata = createPageMetadata({
  title: "Festas a Decorrer",
  description: "Monitorização de festas em tempo real",
});
