import { Metadata } from "next";
import { createPageMetadata } from "@/lib/metadata";

export const metadata: Metadata = createPageMetadata({
  title: "Relatórios",
  description: "Relatórios e análises de festas e reservas",
});