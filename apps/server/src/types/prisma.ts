/**
 * Prisma-specific types used internally by the backend
 * These types are not shared with the frontend as they represent
 * internal database structures and relationships.
 */

// Prisma-specific types will be added here as we implement new modules
// e.g., ReservaWithRelations, FestaWithMonitores, etc.
